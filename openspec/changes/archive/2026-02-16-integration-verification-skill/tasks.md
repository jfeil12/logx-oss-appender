## 1. 技能入口与执行契约落地

- [x] 1.1 盘点并确认当前可复用的验证技能入口（含 `logx-minio-verify` / `/minio-verify`）与调用方式，形成统一触发入口方案
- [x] 1.2 在技能说明中补充“代码编写完成后必须触发统一验证入口”的强制规则与适用范围
- [x] 1.3 明确统一入口在本地与 CI 的一致语义（执行顺序、失败判定、输出摘要字段）

## 2. 真实 MinIO 前置检查与失败阻断

- [x] 2.1 实现并固化真实 MinIO 前置检查项（endpoint 可达、凭据有效、目标桶可用）
- [x] 2.2 约束前置检查失败时快速失败并中止后续验证，返回非零退出码
- [x] 2.3 统一失败输出格式，至少包含失败检查项、原因与排查建议

## 3. 全量集成链路编排与通过标准

- [x] 3.1 明确“项目要求的全量链路”清单（compatibility-tests 全模块与 `jdk21-test`）并写入验证流程
- [x] 3.2 编排全量验证执行顺序，确保只执行子集测试时判定为不通过
- [x] 3.3 固化通过标准：仅当全量链路全部通过且关键模块无缺失时判定通过

## 4. CI 门禁接入与可追溯结果

- [x] 4.1 在 CI 中接入统一验证入口并设置为合并前必过门禁
- [x] 4.2 输出可追溯结果摘要（执行范围、关键命令、成功/失败结论、日志/报告路径）
- [x] 4.3 在门禁失败时保留关键日志与定位信息，支持复现与回归

## 5. 文档与验收收口

- [x] 5.1 更新 AGENTS.md 与相关测试文档，明确“未通过完整集成验证不算完成”的规则
- [x] 5.2 补充本地执行指引（MinIO 准备、环境变量、验证命令、常见失败处理）
- [x] 5.3 执行一次完整验收演练：真实 MinIO 环境 + 全量链路验证，并记录结果作为交付依据
